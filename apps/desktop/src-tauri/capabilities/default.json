{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the IRIS desktop shell window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
